# GC Travel MCP Server — Environment Variables
# Copy to .env for local dev (never commit this file)

# Your deployed URL (set automatically by Railway/Render)
BASE_URL=https://your-app.railway.app

# Port (Railway sets this automatically)
PORT=3000

# Optional: API key to protect your MCP server
# If set, Base44 must send this in the Authorization header
MCP_API_KEY=your_secret_key_here

# Base44 credentials (for when you swap demo data with real Base44 SDK)
BASE44_APP_ID=your_base44_app_id
BASE44_SERVICE_ROLE_KEY=your_base44_service_role_key
